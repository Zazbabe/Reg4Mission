@{
    var candidateId = (int)(ViewBag.CandidateId ?? 0);
}

<div class="space-y-3">
    <div>
        <div class="text-xs text-slate-500">E-post</div>
        <div class="font-semibold text-slate-900">candidate@demo.se</div>
    </div>

    <div>
        <div class="text-xs text-slate-500">Telefon</div>
        <div class="font-semibold text-slate-900">070-123 45 67</div>
    </div>

    <div class="text-xs text-slate-500">
        UI-bara: Blir trackat senare för candidate ID @candidateId.
    </div>
</div>