@{
    var candidateId = (int)(ViewBag.CandidateId ?? 0);
}

<div class="vstack gap-2">
    <div>
        <div class="text-muted small">E-post</div>
        <div class="fw-semibold">candidate@demo.se</div>
    </div>
    <div>
        <div class="text-muted small">Telefon</div>
        <div class="fw-semibold">070-123 45 67</div>
    </div>
    <div class="text-muted small">
        Endast UI: Kommer att trackas för att kandidater ska kunna se @candidateId.
    </div>
</div>